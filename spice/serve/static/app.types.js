"use strict";

// Generated by `spice dev serve-web-types --write` from
// spice.serve.payload.wire. Do not edit this file by hand.

/**
 * @typedef {*} JsonValue
 */

/**
 * @typedef {Object.<string, JsonValue>} ShellSettings
 */

/**
 * @typedef {Object.<string, JsonValue>} RestartRefusal
 */

/**
 * @typedef {(LanePayload|LaneErrorPayload)} LaneWirePayload
 */

/**
 * @typedef {(TaskDrainResult|WorkTreeSendResult)} RoutedResult
 */

/**
 * @typedef {(LaneChromeIdentityFacet|LaneChromeTeamConfigFacet|LaneChromePendingInboxFacet|LaneChromeTaskBoardFacet|LaneChromeLifecycleFacet|LaneChromeRenewalFacet|LaneChromeActivityFacet)} LaneChromeFacet
 */

/**
 * @typedef {(TeamCommandApplied|TeamCommandRefused)} TeamCommandResponse
 */

/**
 * @typedef {(AgentEnsureLaunched|AgentEnsureUnstarted)} AgentEnsurePayload
 */

/**
 * @typedef {Object} MessageAttachment
 * @property {string} name
 * @property {string} contentType
 * @property {number} size
 * @property {string} path
 * @property {string=} url
 */

/**
 * @typedef {Object} AckSegment
 * @property {Array.<string>} keys
 * @property {string} html
 * @property {string} disposition
 */

/**
 * @typedef {Object} PlanItem
 * @property {string} step
 * @property {string} status
 */

/**
 * @typedef {Object} LaneMessage
 * @property {string} key
 * @property {number} index
 * @property {string} timestamp
 * @property {string} kind
 * @property {string} source_kind
 * @property {string} text
 * @property {string} display_text
 * @property {string} display_html
 * @property {string} preamble_html
 * @property {string} preview
 * @property {boolean} image_only
 * @property {number} task_card_count
 * @property {number} ack_count
 * @property {Array.<string>} ack_keys
 * @property {number} nack_count
 * @property {Array.<string>} nack_keys
 * @property {Array.<string>} ack_utterances
 * @property {Array.<AckSegment>} ack_segments
 * @property {Array.<string>} speech_utterances
 * @property {Array.<PlanItem>} plan_items
 */

/**
 * @typedef {Object} AckContext
 * @property {string} key
 * @property {boolean} found
 * @property {string=} text
 * @property {string=} html
 * @property {string=} priority
 * @property {string=} disposition
 * @property {Array.<MessageAttachment>=} attachments
 */

/**
 * @typedef {Object} DriverIdentity
 * @property {string} name
 * @property {string} model
 * @property {string} effort
 */

/**
 * @typedef {Object} AgentIdentity
 * @property {string} state
 * @property {string=} name
 */

/**
 * @typedef {Object} ThreadIdentity
 * @property {string} state
 * @property {string=} threadId
 * @property {string=} error
 */

/**
 * @typedef {Object} TargetIdentity
 * @property {string} branch
 * @property {DriverIdentity} driver
 * @property {AgentIdentity} agent
 * @property {ThreadIdentity} thread
 * @property {string=} targetId
 * @property {string=} worktreeName
 */

/**
 * @typedef {Object} ServeTargetIdentity
 * @property {string} id
 * @property {string} worktreeName
 * @property {string} repoRoot
 * @property {string} branch
 */

/**
 * @typedef {Object} ServeAgentDriverIdentity
 * @property {string} desired
 * @property {string} actual
 * @property {string} transcriptOwner
 */

/**
 * @typedef {Object} ServeAgentLaunchFacts
 * @property {string=} model
 * @property {string=} effort
 * @property {string=} serviceTier
 * @property {string=} source
 */

/**
 * @typedef {Object} ServeAgentLaunchIdentity
 * @property {ServeAgentLaunchFacts} desired
 * @property {ServeAgentLaunchFacts} actual
 */

/**
 * @typedef {Object} ServeRenewalIdentity
 * @property {string} state
 * @property {(number|null)} teamIndex
 * @property {string} ancestorThreadId
 * @property {string} successorThreadId
 * @property {number} revision
 */

/**
 * @typedef {Object} ServeAgentIdentity
 * @property {ServeAgentDriverIdentity} driver
 * @property {ThreadIdentity} thread
 * @property {ServeAgentLaunchIdentity} launch
 * @property {string=} actorId
 * @property {ServeTargetIdentity=} target
 * @property {ServeRenewalIdentity=} renewal
 */

/**
 * @typedef {Object} TeamIdentity
 * @property {string} state
 * @property {string=} teamId
 * @property {number=} teamRevision
 * @property {number=} configRevision
 */

/**
 * @typedef {Object} TaskFilterEntry
 * @property {string} project
 * @property {string} source
 */

/**
 * @typedef {Object} TeamAgentIdentity
 * @property {string=} actorId
 * @property {string=} targetId
 * @property {string=} threadId
 * @property {string=} driverName
 * @property {string=} driverModel
 * @property {string=} driverEffort
 * @property {string=} actualDriver
 * @property {string=} actualModel
 * @property {string=} actualEffort
 * @property {string=} actualServiceTier
 * @property {string=} desiredDriver
 * @property {string=} desiredModel
 * @property {string=} desiredEffort
 * @property {string=} transcriptOwner
 * @property {string=} renewalState
 * @property {string=} renewalAncestorThreadId
 * @property {string=} renewalSuccessorThreadId
 * @property {number=} renewalRevision
 * @property {number=} updatedAt
 */

/**
 * @typedef {Object} RenewalIntentPayload
 * @property {string=} agentId
 * @property {boolean=} requested
 * @property {string=} state
 * @property {string=} teamId
 * @property {string=} ancestorThreadId
 * @property {string=} successorAgentId
 * @property {string=} successorThreadId
 * @property {(number|null)=} teamSlot
 * @property {TeamAgentIdentity=} predecessorIdentity
 * @property {TeamAgentIdentity=} successorIdentity
 * @property {number=} revision
 */

/**
 * @typedef {Object} TaskFilterRecord
 * @property {string} name
 * @property {string} primaryStem
 * @property {number=} openTaskCount
 * @property {number=} readyTaskCount
 * @property {number=} inFlightTaskCount
 * @property {number=} blockedTaskCount
 * @property {number=} deferredTaskCount
 */

/**
 * @typedef {Object} TaskFilterStem
 * @property {string} name
 * @property {Array.<string>} filters
 * @property {number=} openTaskCount
 * @property {number=} readyTaskCount
 * @property {number=} inFlightTaskCount
 * @property {number=} blockedTaskCount
 * @property {number=} deferredTaskCount
 * @property {number=} waitingTaskCount
 * @property {number=} oopsTaskCount
 */

/**
 * @typedef {Object} TaskFilterCatalog
 * @property {Array.<string>} approvedStems
 * @property {Array.<string>} hiddenStems
 * @property {Array.<string>} approvedPhases
 * @property {Array.<string>} defaultFlow
 * @property {Object.<string, Array.<string>>} perStemFlows
 * @property {string} hiddenProjectPrefix
 * @property {string} filterDelimiter
 * @property {string} segmentPattern
 * @property {string} segmentRuleLabel
 * @property {Array.<string>} filterExamples
 */

/**
 * @typedef {Object} TaskFilterInventory
 * @property {string=} revision
 * @property {Array.<TaskFilterRecord>=} filters
 * @property {Array.<TaskFilterStem>=} primaryStems
 * @property {number=} openTaskCount
 * @property {TaskFilterCatalog=} catalog
 */

/**
 * @typedef {Object} LaneInfoRow
 * @property {string} key
 * @property {string} value
 * @property {boolean} span
 */

/**
 * @typedef {Object} LaneInfoMember
 * @property {string} targetId
 * @property {Array.<LaneInfoRow>} rows
 */

/**
 * @typedef {Object} ReviewPressureItem
 * @property {string} reviewedTask
 * @property {string} finding
 * @property {string} findingSeverity
 * @property {string} reviewer
 * @property {string} source
 * @property {number} followupCount
 * @property {string} reviewedAt
 */

/**
 * @typedef {Object} ReviewPressure
 * @property {number} count
 * @property {number} openFollowupCount
 * @property {Array.<ReviewPressureItem>} items
 */

/**
 * @typedef {Object} LaneInfo
 * @property {Array.<LaneInfoRow>} summaryRows
 * @property {Array.<LaneInfoMember>} members
 * @property {ReviewPressure=} reviewPressure
 */

/**
 * @typedef {Object} ClaimedTask
 * @property {string=} handle
 * @property {string=} phase
 * @property {string=} title
 */

/**
 * @typedef {Object} StatusLine
 * @property {string=} bindingStatus
 * @property {boolean=} bound
 * @property {string=} bindingError
 * @property {string=} rolloutStatus
 * @property {string=} activityStatus
 * @property {string=} latestActivityKind
 * @property {string=} latestMessagePreview
 * @property {string=} latestActivityPreview
 * @property {string=} preview
 * @property {string=} agentProcessStatus
 * @property {string=} agentVisualStatus
 * @property {ClaimedTask=} claimedTask
 * @property {string=} error
 */

/**
 * @typedef {Object} LaneMetrics
 * @property {number=} drained
 * @property {number=} acked
 * @property {number=} sends
 * @property {number=} toolCalls
 * @property {number=} uptimeSeconds
 * @property {Array.<number>=} sparkline
 */

/**
 * @typedef {Object} AgentEnsureLaunched
 * @property {true} ok
 * @property {string} threadId
 * @property {string=} action
 * @property {string=} trigger
 * @property {string=} taskHandle
 * @property {string=} provider
 * @property {string=} status
 * @property {number=} pid
 * @property {number=} processGroupId
 * @property {string=} serviceTier
 * @property {string=} readyAt
 * @property {string=} startupFailure
 * @property {string=} prompt
 * @property {string=} logPath
 * @property {undefined=} reason
 * @property {undefined=} retryAfterSeconds
 * @property {undefined=} claimReleased
 * @property {undefined=} failure
 * @property {undefined=} error
 * @property {undefined=} restartRefusal
 * @property {undefined=} deadletteredInboxKeys
 * @property {undefined=} deadletteredInboxKey
 * @property {undefined=} deadletterRequeueCommand
 */

/**
 * @typedef {Object} AgentEnsureUnstarted
 * @property {boolean=} ok
 * @property {string=} action
 * @property {string=} trigger
 * @property {string=} taskHandle
 * @property {string=} reason
 * @property {number=} retryAfterSeconds
 * @property {boolean=} claimReleased
 * @property {string=} failure
 * @property {string=} error
 * @property {RestartRefusal=} restartRefusal - driver-specific launch refusal facts
 * @property {Array.<string>=} deadletteredInboxKeys
 * @property {string=} deadletteredInboxKey
 * @property {string=} deadletterRequeueCommand
 * @property {undefined=} threadId
 * @property {undefined=} provider
 * @property {undefined=} status
 * @property {undefined=} pid
 * @property {undefined=} processGroupId
 * @property {undefined=} serviceTier
 * @property {undefined=} readyAt
 * @property {undefined=} startupFailure
 * @property {undefined=} prompt
 * @property {undefined=} logPath
 */

/**
 * @typedef {Object} AgentStatusPayload
 * @property {boolean} ok
 * @property {string} provider
 * @property {string} workTreeId
 * @property {string} status
 * @property {number} pid
 * @property {number} processGroupId
 * @property {string} threadId
 * @property {string} model
 * @property {string} effort
 * @property {string} serviceTier
 * @property {boolean} launchable
 * @property {string} bindingStatus
 * @property {string} bindingError
 * @property {RestartRefusal=} restartRefusal - driver-specific launch refusal facts
 * @property {string=} readyAt
 * @property {string=} startupFailure
 */

/**
 * @typedef {Object} LanePayload
 * @property {Array.<LaneMessage>} messages
 * @property {Array.<AckContext>} ackContexts
 * @property {TargetIdentity} targetIdentity
 * @property {ServeAgentIdentity} serveAgentIdentity
 * @property {LaneInfo} laneInfo
 * @property {AgentEnsurePayload} agentEnsure
 * @property {StatusLine} statusLine
 * @property {string} error
 * @property {LaneChromePayload} chrome
 * @property {string=} agentProcessStatus
 * @property {Array.<string>=} removedMessageKeys
 */

/**
 * @typedef {Object} PendingLanePayload
 * @property {LaneChromePayload} chrome
 */

/**
 * @typedef {Object} LaneErrorPayload
 * @property {string} error
 * @property {Array.<LaneMessage>} messages
 * @property {StatusLine} statusLine
 */

/**
 * @typedef {Object} LaneChromeFacetOrder
 * @property {string} epoch
 * @property {number} revision
 */

/**
 * @typedef {Object} LaneChromeIdentity
 * @property {string} displayName
 * @property {ServeTargetIdentity} target
 * @property {ServeAgentDriverIdentity} driver
 * @property {ThreadIdentity} thread
 * @property {ServeAgentLaunchIdentity} launch
 * @property {string=} actorId
 * @property {string=} agentName
 */

/**
 * @typedef {Object} LaneChromeTeamConfig
 * @property {TeamIdentity} teamIdentity
 */

/**
 * @typedef {Object} LaneChromePendingInbox
 * @property {number} count
 * @property {string} label
 * @property {Array.<string>} keys
 */

/**
 * @typedef {Object} LaneChromeTaskBoard
 * @property {Array.<string>} taskFilters
 * @property {Array.<TaskFilterEntry>} taskFilterEntries
 * @property {Array.<string>} effectiveTaskFilters
 * @property {TaskFilterInventory} taskFilterInventory
 * @property {number} privateTaskCount
 * @property {ReviewPressure=} reviewPressure
 * @property {ClaimedTask=} claimedTask
 */

/**
 * @typedef {Object} LaneChromeLifecycle
 * @property {string} processStatus
 * @property {string=} visualStatus
 * @property {string=} bindingStatus
 * @property {string=} rolloutStatus
 */

/**
 * @typedef {Object} LaneChromeRenewal
 * @property {string} lifetime
 * @property {RenewalIntentPayload} renewalIntent
 */

/**
 * @typedef {Object} LaneChromeActivity
 * @property {string} lastAssistantAt
 * @property {string=} latestActivityKind
 * @property {string=} latestMessagePreview
 * @property {string=} latestActivityPreview
 * @property {string=} preview
 */

/**
 * @typedef {Object} LaneChromeIdentityFacet
 * @property {"target-registry"} authority
 * @property {LaneChromeFacetOrder} order
 * @property {(LaneChromeIdentity|null)} value
 */

/**
 * @typedef {Object} LaneChromeTeamConfigFacet
 * @property {"team-store"} authority
 * @property {LaneChromeFacetOrder} order
 * @property {(LaneChromeTeamConfig|null)} value
 */

/**
 * @typedef {Object} LaneChromePendingInboxFacet
 * @property {"inbox"} authority
 * @property {LaneChromeFacetOrder} order
 * @property {(LaneChromePendingInbox|null)} value
 */

/**
 * @typedef {Object} LaneChromeTaskBoardFacet
 * @property {"task-board"} authority
 * @property {LaneChromeFacetOrder} order
 * @property {(LaneChromeTaskBoard|null)} value
 */

/**
 * @typedef {Object} LaneChromeLifecycleFacet
 * @property {"lifecycle-reconciler"} authority
 * @property {LaneChromeFacetOrder} order
 * @property {(LaneChromeLifecycle|null)} value
 */

/**
 * @typedef {Object} LaneChromeRenewalFacet
 * @property {"team-store"} authority
 * @property {LaneChromeFacetOrder} order
 * @property {(LaneChromeRenewal|null)} value
 */

/**
 * @typedef {Object} LaneChromeActivityFacet
 * @property {"transcript"} authority
 * @property {LaneChromeFacetOrder} order
 * @property {(LaneChromeActivity|null)} value
 */

/**
 * @typedef {Object} LaneChromePayload
 * @property {string} targetId
 * @property {LaneChromeIdentityFacet=} identity
 * @property {LaneChromeTeamConfigFacet=} teamConfig
 * @property {LaneChromePendingInboxFacet=} pendingInbox
 * @property {LaneChromeTaskBoardFacet=} taskBoard
 * @property {LaneChromeLifecycleFacet=} lifecycle
 * @property {LaneChromeRenewalFacet=} renewal
 * @property {LaneChromeActivityFacet=} activity
 */

/**
 * @typedef {Object} LaneChromeSourcePayload
 * @property {TargetIdentity=} targetIdentity
 * @property {ServeAgentIdentity=} serveAgentIdentity
 * @property {LaneInfo=} laneInfo
 * @property {StatusLine=} statusLine
 * @property {LaneChromePayload=} chrome
 */

/**
 * @typedef {Object} WorkTreePayload
 * @property {string} id
 * @property {string} repoRoot
 * @property {string} displayName
 * @property {string} branch
 * @property {string} agentProcessStatus
 * @property {TargetIdentity} targetIdentity
 * @property {ServeAgentIdentity} serveAgentIdentity
 * @property {LaneInfo} laneInfo
 * @property {AgentEnsurePayload} agentEnsure
 * @property {string} agentVisualStatus
 * @property {StatusLine} statusLine
 * @property {LaneChromePayload} chrome
 */

/**
 * @typedef {Object} TargetsPayload
 * @property {Array.<WorkTreePayload>} workTrees
 * @property {string} defaultTargetId
 * @property {Array.<string>=} observerErrors
 * @property {Array.<string>=} targetsDiscoveryErrors
 */

/**
 * @typedef {Object} TeamGlobalSettings
 * @property {boolean} fastMode
 * @property {boolean=} observerMode
 */

/**
 * @typedef {Object} TeamConfigPayload
 * @property {string} lifetime
 * @property {Array.<string>} taskFilters
 * @property {Array.<TaskFilterEntry>} taskFilterEntries
 * @property {Array.<string>=} effectiveTaskFilters
 * @property {ShellSettings=} shellSettings - user-defined team shell preferences
 * @property {number=} revision
 */

/**
 * @typedef {Object} TeamSplitBack
 * @property {boolean} available
 * @property {number} memberCount
 */

/**
 * @typedef {Object} TeamMemberPayload
 * @property {string} agentId
 * @property {RenewalIntentPayload} renewalIntent
 * @property {Array.<string>=} agentAliases
 * @property {TeamAgentIdentity=} agentFacts
 */

/**
 * @typedef {Object} TeamPayload
 * @property {string} teamId
 * @property {number} revision
 * @property {TeamConfigPayload} config
 * @property {Array.<TeamMemberPayload>} members
 * @property {TeamSplitBack} splitBack
 * @property {string=} status
 * @property {number=} order
 */

/**
 * @typedef {Object} TeamSnapshot
 * @property {number} globalRevision
 * @property {TeamGlobalSettings} globalSettings
 * @property {Array.<TeamPayload>} teams
 * @property {number=} teamCount
 * @property {Array.<string>=} removedTeamIds
 */

/**
 * @typedef {Object} TeamSnapshotResponse
 * @property {number} revision
 * @property {boolean} changed
 * @property {boolean=} ok
 * @property {boolean=} differential
 * @property {TeamSnapshot=} snapshot
 */

/**
 * @typedef {Object} TeamCommandApplied
 * @property {true} ok
 * @property {number} revision
 * @property {boolean} differential
 * @property {TeamSnapshot} snapshot
 */

/**
 * @typedef {Object} TeamCommandRefused
 * @property {false} ok
 * @property {string} error
 */

/**
 * @typedef {Object} MetricSeriesSubject
 * @property {Array.<string>} agentIds
 * @property {string=} agentId
 * @property {string=} teamId
 */

/**
 * @typedef {Object} MetricSeriesPoint
 * @property {number} bucketStart
 * @property {number} value
 * @property {number=} messages
 * @property {number=} sends
 * @property {number=} acks
 * @property {number=} claimed
 * @property {number=} active
 * @property {number=} completed
 * @property {number=} drained
 * @property {string=} agentId
 * @property {number=} share
 * @property {number=} work
 * @property {string=} taskId
 * @property {string=} handle
 * @property {string=} title
 * @property {string=} phase
 * @property {number=} phaseIndex
 * @property {string=} threadId
 * @property {string=} teamId
 * @property {string=} driver
 * @property {string=} model
 * @property {string=} effort
 * @property {(number|null)=} startedAt
 * @property {(number|null)=} endedAt
 * @property {number=} wallSeconds
 * @property {number=} inputTokens
 * @property {number=} cachedInputTokens
 * @property {number=} outputTokens
 * @property {number=} reasoningOutputTokens
 * @property {number=} totalTokens
 * @property {number=} turns
 * @property {number=} renewals
 * @property {Array.<string>=} sourceFiles
 * @property {boolean=} partial
 * @property {Array.<string>=} partialMarkers
 */

/**
 * @typedef {Object} MetricSeriesPayload
 * @property {boolean} ok
 * @property {string} metric
 * @property {string} lens
 * @property {number} start
 * @property {number} effectiveStart
 * @property {number} end
 * @property {number} bucketSeconds
 * @property {MetricSeriesSubject} subject
 * @property {Array.<MetricSeriesPoint>} points
 */

/**
 * @typedef {Object} SubmissionStage
 * @property {string} at
 * @property {string} source
 * @property {string} evidence
 * @property {string=} sourceAt
 */

/**
 * @typedef {Object} SubmissionLifecycle
 * @property {string} key
 * @property {string} stage
 * @property {string} disposition
 * @property {Object.<string, SubmissionStage>} stages
 * @property {Object.<string, number>} durationsMs
 */

/**
 * @typedef {Object} ServerTiming
 * @property {number=} mutationQueueMs
 * @property {number=} targetResolveMs
 * @property {number=} sendPayloadMs
 * @property {number=} totalBeforeReplyMs
 * @property {number=} replyLockWaitMs
 * @property {number=} replyLockHoldMs
 * @property {number=} replyWriteMs
 * @property {number=} totalMs
 */

/**
 * @typedef {Object} WorkTreeRoute
 * @property {string} actor
 * @property {TargetIdentity} targetIdentity
 * @property {ServeAgentIdentity} serveAgentIdentity
 * @property {Array.<string>} memberAgents
 * @property {LaneChromePayload} chrome
 */

/**
 * @typedef {Object} WorkTreeSendResult
 * @property {boolean} ok
 * @property {string=} error
 * @property {string=} key
 * @property {string=} path
 * @property {string=} text
 * @property {string=} requestText
 * @property {Array.<string>=} requestControls
 * @property {string=} requestPriority
 * @property {string=} requestHtml
 * @property {boolean=} noSay
 * @property {Array.<MessageAttachment>=} attachments
 * @property {AgentEnsurePayload=} agentEnsure
 * @property {WorkTreeRoute=} route
 * @property {ServerTiming=} serverTiming
 * @property {SubmissionLifecycle=} submission
 * @property {LaneChromePayload=} chrome
 */

/**
 * @typedef {Object} TaskDrainResult
 * @property {boolean} ok
 * @property {WorkTreeRoute=} route
 * @property {string=} error
 */

/**
 * @typedef {Object} WatchTiming
 * @property {number} changeDetectedWallMs
 * @property {number} preSendWallMs
 * @property {number} detectToSendMs
 * @property {number} signatureMs
 * @property {number} payloadMs
 */

/**
 * @typedef {Object} FrameTelemetry
 * @property {number} count
 * @property {number} bytes
 * @property {number} sendLockWaitMsTotal
 * @property {number} sendLockWaitMsLast
 * @property {number} sendLockWaitMsMax
 * @property {number} sendLockHoldMsTotal
 * @property {number} sendLockHoldMsLast
 * @property {number} sendLockHoldMsMax
 */

/**
 * @typedef {Object} FrameTotals
 * @property {number} count
 * @property {number} bytes
 */

/**
 * @typedef {Object} LiveBusDiagnostics
 * @property {string} clientId
 * @property {Object.<string, FrameTelemetry>} frames
 * @property {FrameTotals} totals
 */

/**
 * @typedef {Object} LaneSubscriptionPayload
 * @property {string} targetId
 * @property {LaneWirePayload} payload
 * @property {string} subscriptionGeneration
 * @property {boolean} watcherActive
 * @property {string} watcherError
 */

/**
 * @typedef {Object} LaneAppendPayload
 * @property {Array.<LaneMessage>} messages
 * @property {Array.<AckContext>=} ackContexts
 * @property {Array.<string>=} removedMessageKeys
 */

/**
 * @typedef {Object} LaneAppendFrame
 * @property {"lane.append"} type
 * @property {string} targetId
 * @property {LaneAppendPayload} payload
 */

/**
 * @typedef {Object} DirtyLane
 * @property {string} targetId
 * @property {string} subscriptionGeneration
 */

/**
 * @typedef {Object} BusErrorFrame
 * @property {"bus.error"} type
 * @property {string} error
 * @property {string=} requestId
 */

/**
 * @typedef {Object} BusPongFrame
 * @property {"bus.pong"} type
 * @property {LiveBusDiagnostics} diagnostics
 * @property {string=} requestId
 */

/**
 * @typedef {Object} TargetsFrame
 * @property {"targets.payload"} type
 * @property {TargetsPayload} payload
 * @property {string=} requestId
 */

/**
 * @typedef {Object} TeamsFrame
 * @property {"teams.payload"} type
 * @property {TeamSnapshotResponse} payload
 * @property {string=} requestId
 */

/**
 * @typedef {Object} TeamCommandFrame
 * @property {"teams.commandResult"} type
 * @property {TeamCommandResponse} result
 * @property {string=} requestId
 */

/**
 * @typedef {Object} LanesFrame
 * @property {"lanes.payload"} type
 * @property {Array.<LaneSubscriptionPayload>} lanes
 * @property {string=} requestId
 */

/**
 * @typedef {Object} LaneFrame
 * @property {"lane.payload"} type
 * @property {LaneWirePayload} payload
 * @property {string=} requestId
 * @property {string=} targetId
 * @property {string=} source
 * @property {string=} subscriptionGeneration
 * @property {WatchTiming=} watchTiming
 */

/**
 * @typedef {Object} LaneConfiguredFrame
 * @property {"lane.configured"} type
 * @property {string=} requestId
 */

/**
 * @typedef {Object} LanesDirtyFrame
 * @property {"lanes.dirty"} type
 * @property {Array.<DirtyLane>} lanes
 */

/**
 * @typedef {Object} LaneUnsubscribedFrame
 * @property {"lane.unsubscribed"} type
 * @property {string=} requestId
 */

/**
 * @typedef {Object} LaneSendResultFrame
 * @property {"lane.sendResult"} type
 * @property {WorkTreeSendResult} result
 * @property {string=} requestId
 */

/**
 * @typedef {Object} LaneSendTimingFrame
 * @property {"lane.sendTiming"} type
 * @property {string} requestId
 * @property {ServerTiming} serverTiming
 */

/**
 * @typedef {Object} LaneTaskDrainFrame
 * @property {"lane.taskDrainResult"} type
 * @property {TaskDrainResult} result
 * @property {string=} requestId
 */

/**
 * @typedef {Object} MetricSeriesFrame
 * @property {"metrics.seriesResult"} type
 * @property {MetricSeriesPayload} result
 * @property {string=} requestId
 */

/**
 * @typedef {Object} MetricsSummaryFrame
 * @property {"metrics.summaryResult"} type
 * @property {LaneMetrics} result
 * @property {string=} requestId
 */

/**
 * @typedef {Object} LanePendingFrame
 * @property {"lane.pending"} type
 * @property {string} targetId
 * @property {string} source
 * @property {string} subscriptionGeneration
 * @property {PendingLanePayload} payload
 */

/**
 * @typedef {Object} LaneSubmissionFrame
 * @property {"lane.submission"} type
 * @property {string} targetId
 * @property {string} source
 * @property {string} subscriptionGeneration
 * @property {SubmissionLifecycle} submission
 */

/**
 * @typedef {Object} ServeBranding
 * @property {string} name
 * @property {string} defaultLifetime
 * @property {string} version
 */

/**
 * @typedef {Object} ServeInitialGlobalSettings
 * @property {boolean} fastMode
 * @property {boolean} observerMode
 */

/** @type {ServeBranding} */
var spiceServeBranding;

/** @type {ServeInitialGlobalSettings} */
var spiceServeInitialGlobalSettings;

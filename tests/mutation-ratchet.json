{
  "modules": {
    "spice/config/layers.py": {
      "killed": 18,
      "mutants": 20,
      "score": 0.9,
      "survived": 2,
      "timed_out": 0
    }
  },
  "standing": {
    "equivalentMutants": [
      {
        "mutationIndex": 13,
        "path": "spice/config/layers.py",
        "reason": "reversed validation zip strictness is equivalent because parsed values and layers are appended together in the same loop"
      },
      {
        "mutationIndex": 14,
        "path": "spice/config/layers.py",
        "reason": "merge zip strictness is equivalent because parsed values and layers are appended together in the same loop"
      }
    ],
    "lowInformationMutants": [
      {
        "mutationIndex": 16,
        "path": "spice/config/layers.py",
        "reason": "invalid Path multiplication aborts the suite before pytest can attribute the kill to an individual node ID"
      }
    ],
    "maxMutantsPerModule": 20,
    "retainedZeroConstraintTests": {
      "tests/test_configlayer.py::test_parse_error_names_the_exact_layer_and_path[system]": "Retained as the public system-layer parse-error contract; the selected path-arithmetic crash is killed before node-ID attribution, so adding implementation-inspection assertions would create no behavioral constraint."
    },
    "surface": "spice dev doctor",
    "targets": [
      "spice/config/layers.py"
    ],
    "tests": [
      "tests/test_configlayer.py"
    ],
    "timeoutSeconds": 30
  },
  "version": 1
}
